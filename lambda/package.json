{
  "name": "devops-knowledgeops-lambda",
  "version": "1.0.0",
  "description": "Lambda functions for DevOps KnowledgeOps Agent",
  "private": true,
  "workspaces": [
    "actions",
    "auth",
    "bedrock",
    "chat-processor",
    "memory",
    "session",
    "types",
    "utils"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist */dist",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock": "^3.450.0",
    "@aws-sdk/client-bedrock-agent": "^3.450.0",
    "@aws-sdk/client-bedrock-agent-runtime": "^3.450.0",
    "@aws-sdk/client-bedrock-runtime": "^3.910.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.450.0",
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0"
  }
}
